var InspiroBot = require("./index.js")
var inspirobot = new InspiroBot()


function handleQuote(error, qoute)
{
  if (error)
  {
    console.err(error)
  }
  else {
    console.log(qoute.url)
    setTimeout(getQuote, 1000);
  }
}


function getQuote()
{
  inspirobot.fetchQuote(handleQuote)
}

inspirobot.fetchQuote(handleQuote)
